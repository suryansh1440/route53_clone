import { ComingSoonPlaceholder } from '@/components/aws/ComingSoonPlaceholder';

export default function TrafficPoliciesPage() {
  return (
    <ComingSoonPlaceholder
      title="Traffic policies"
      description="Use traffic flow visual policy editor to create complex routing configurations for your domain."
      featureName="Traffic Policies"
    />
  );
}
